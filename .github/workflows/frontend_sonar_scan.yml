The file on the current main/develop branches already uses GitHub Secrets properly (`${{ secrets.SONAR_TOKEN }}`). However, the token `ec4183646e59dd70c8077acfabe52062ccbea7a9` remains exposed in git history at commits `7bebf5186eab1527acc202978401c3607b306e5b` (line 44, commented-out docker section) and `6ba37d8c62c37979d88faae1d98082380721e21c` (line 22, active docker section).

The current clean version on main is:

```yaml
name: SonarQube Static Analysis

on:
  push:
    paths:
      - govtool/frontend/**
      - .github/workflows/frontend_sonar_scan.yml

jobs:
  execute_sonar_scanner:
    name: Execute sonar-scanner on govtool frontend
    runs-on: ubuntu-latest
    permissions: read-all
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: govtool/frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('govtool/frontend/package-lock.json') }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: "govtool/frontend/.nvmrc"
          registry-url: "https://registry.npmjs.org/"
          scope: "@intersect.mbo"

      - name: 🧪 Test
        working-directory: govtool/frontend
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPMRC_TOKEN }}
          NODE_OPTIONS: "--max_old_space_size=4096"
        run: |
          npm ci
          npm run test:coverage

      - uses: sonarsource/sonarqube-scan-action@master
        if: always()
        with:
          projectBaseDir: govtool/frontend
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
```

No diff needed for this file — it is already clean on the current branch. The remaining work is preventive tooling and history scrubbing (operational).